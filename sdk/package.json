{
  "name": "@stbr/sss-token",
  "version": "0.1.0",
  "description": "TypeScript SDK for the Solana Stablecoin Standard (SSS) backend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "files": ["dist"],
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
